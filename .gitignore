/target
**/*.rs.bk
Cargo.lock

.env
.env.local
.venv/
.venv-shell/

.DS_Store
Thumbs.db

.idea/
.vscode/
*.iml
.fleet/

*.swp
*.swo
*~

# Python build artefacts (PyO3 + maturin)
__pycache__/
*.py[cod]
*$py.class
*.abi3.so
*.so

# Notebook side-effect (the canonical figure lives inside the .ipynb)
notebooks/audit_three_panels.png

# Striatus shell demo per-iteration dumps (large; final.npy may be committed selectively)
crates/umst-concrete-cartridge/examples/_artifacts/shell/iter_*.npy

# Local optimisation / agent logs
opt*.log
rust_out
