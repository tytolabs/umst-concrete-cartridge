# SPDX-License-Identifier: MIT
# Copyright (c) 2026 Santhosh Shyamsundar, Santosh Prabhu Shenbagamoorthy — Studio TYTO
#
# FLEET-COMPOSER-ACCEL-D AC105 — umst-concrete composite durability witness.
# Standalone write-set crate @ physics/durability* — no mod.rs mutation.

[package]
name = "umst-concrete-durability-witness"
version = "0.0.0"
edition = "2021"
publish = false
license = "MIT"
description = "Composite durability adoption witness (frost + chloride + healing weakest-link)"

[lib]
path = "lib.rs"

[dependencies]
burn = { version = "=0.13.2", default-features = false, features = ["ndarray"] }
burn-ndarray = "=0.13.2"
umst-concrete-cartridge = { path = "../../../" }

# Standalone write-set crate — not a member of umst-concrete-cartridge workspace.
[workspace]
